function mostrar()
{
/*	let respuesta = "si";
	//acumuladores cuando necesito que mis datos numericos de distintos o igual valor se sumen
	let acumulador; // siempre inicializar en 0. acumulador = acumulador + datonumerico
	//contador para cuando quiero que sume el mismo valor por vuelta
	let contador; // inicializar en 0. contador++;
	let mensaje;
	
	acumulador =0;
	contador =0;

	while (respuesta == "si") 
	{
		//while para validar
			= prompt("  ");
		while(    != "" &&    != "" &&  != "")
		{
    		= prompt("Re    ");
		}
		//while para rangos, solo numeros.
			= parseInt(prompt("    "));
		while(  <  ||   > )	
		{
    		= parseInt(prompt("Re    "));
		}
		// No permitir negativos y ceros, sin el igual permitimos el 0, tambien se puede un <1 y no toma el 0
		    = parseInt(prompt)
		while(  <=0  )	
		{
    		= parseInt(prompt("Re    "));
		}
		
		//switch para acumuladores y contadores
		//cuando hay varios casos solamente
		switch(    )
		{
			case     :

				break;
			case    :

				break;
			case     :

				break;
			default:

				break;
		}	

		// MAYOR 
		if( e > z || bandera == 0) //bandera siempre == 0
		{
    		z = e; // actualiza el valor del maximo
    		bandera = 1; //cambiar valor de bandera
			= //datos de variables que quiera cambiar
		}
		// MENOR
		if( e < z || bandera == 0) // bandera siempre == 0
		{
    		z = e; //actualiza el valor del minimo
    		bandera = 1; //cambiar valor de bandera
			= //datos de variables que quiera cambiar
		}

			respuesta = prompt("Desea continuar?");
	}
	// si uno es mayor que otro. por fuera del while
	if(1 > 2 && 1 > 3)
	{
		= "1";

	}
	else
	{
		if(2 > 3)
		{
			= "2";
		}
		else
		{
			= "3";
		}
	}
	// si uno es menor que otro fuera del while
	if(1 < 2 && 1 < 3)
	{
		= "1";

	}
	else
	{
		if(2 < 3)
		{
			= "2";
		}
		else
		{
			= "3";
		}
	}
	// el promedio de saca así
	promedio = totalSinDescuento - (totalSinDescuento * .descuento )
	//porcentaje = (15 * 200) / 100

	mensaje = ""+"<br>"+
	document.write(mensaje);
	

	// como sacar el porcentaje 
	porcentajeEquipaje = 100-((contadorPasajerosTotal-contadorEquipajeMano)/contadorPasajerosTotal*100);


	La función x% lo que hace es dividir la x por 100, por lo cual tu estas haciendo 4/(1/100) que seria 4/0.01 lo cual da 400.

	Para calcular el porcentaje lo puedes hacer multiplicando simplemente, aquí te dejo unos ejemplos:

	El 1% de 4 = 4(1/100) = 40.01 = 0.04
	El 5% de 2 = 2(5/100) = 20.05 = 0.1
	El 120% de 8 = 8(120/100) = 81.2 = 9.6
*/
}